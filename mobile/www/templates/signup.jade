.ui-status-bar
.ui-container
	.ui-main
		.ui-nav
			.button.back(ui-sref="welcome")
			.text Signup
			.menu
		.ui-input.name
			input.input(placeholder="What's your name?", ng-model="name" type="text")
		.ui-input.email
			input.input(placeholder="What's your email?", ng-model="email" type="text")
		.ui-input.username
			input.input(placeholder="Choose a username", ng-model="username" type="text")
		.ui-input.password
			input.input(placeholder="Choose a password", ng-model="password" type="password")
		.ui-input.password
			input.input(placeholder="Repeat your password", type="password")
		.ui-button.medium.full(ng-click="createUser()") Signup
		.welcome-signup 
			| Already have an account? 
			a(class="ui-fake-link", ui-sref="login") Login.